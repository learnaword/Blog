spring:
  application:
    name: /

  mvc:
    view:
      suffix: .jsp
      prefix: /WEB-INF/views

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
    locale: zh_CN

  profiles:
    active:  @profile.active@
